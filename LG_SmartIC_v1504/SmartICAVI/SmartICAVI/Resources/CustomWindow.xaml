<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    x:Class="SmartICAVI.Resources.CustomWindow">

    <RadialGradientBrush x:Key="RGBrushLightSkyBlue" RadiusX="3" RadiusY="2" GradientOrigin="0, 0">
        <GradientStop Color="White" Offset="0"/>
        <GradientStop Color="LightSkyBlue" Offset="0.8"/>
    </RadialGradientBrush>

    <RadialGradientBrush x:Key="RGBrushRed" RadiusX="3" RadiusY="2" GradientOrigin="0, 0">
        <GradientStop Color="Orange" Offset="0"/>
        <GradientStop Color="Red" Offset="0.3"/>
        <GradientStop Color="DarkRed" Offset="0.4"/>
    </RadialGradientBrush>

    <LinearGradientBrush x:Key="LGBrushDrakBlue" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="DarkSlateBlue" Offset="0"/>
        <GradientStop Color="Black" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="LGBrushKhaki" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="DarkKhaki" Offset="0"/>
        <GradientStop Color="DarkOliveGreen" Offset="1"/>
    </LinearGradientBrush>

    <Style x:Key="StyleCloseButton" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="{StaticResource RGBrushRed}"/>
        <Setter Property="BorderBrush" Value="{StaticResource RGBrushRed}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Padding" Value="15, 5"/>
        <Style.Triggers>
            <Trigger Property="IsFocused" Value="True">
                <Setter Property="Background" Value="{StaticResource RGBrushRed}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <ControlTemplate x:Key="TemplateCustomWindow" TargetType="{x:Type Window}">
        <Border Name="windowFrame" BorderThickness="1" Margin="15" BorderBrush="SlateBlue"
                Width="Auto" Height="Auto"
                CornerRadius="10, 10, 0, 10" Background="{TemplateBinding BorderBrush }">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="5"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!--The title bar-->
                <TextBlock Margin="1" Padding="5" Text="{TemplateBinding Title}" FontWeight="Bold" Foreground="White"
                           MouseLeftButtonDown="titlebar_MouseLeftButtonDown">
                </TextBlock>
                <!--Close Button-->
                <Button Name="btnClose" HorizontalAlignment="Right" Click="cmdClose_Click" Margin="5" Style="{StaticResource StyleCloseButton}">
                    X
                </Button>

                <!-- The Window Content. -->
                <Border Grid.Row="1" Background="{TemplateBinding Background }">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <AdornerDecorator>
                            <ContentPresenter>

                            </ContentPresenter>
                        </AdornerDecorator>

                        <!--<StackPanel Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Bottom" Orientation="Horizontal">
                            <Button Name="btnCancel" Content="Cancel" Margin="15, 5" Padding="15, 10" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="Cancel_Click"/>
                            <Button Name="btnOK" Content="OK" Margin="5" Padding="15, 10" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="OK_Click"/>
                        </StackPanel>-->
                    </Grid>
                </Border>

                <!-- The footer -->
                <ContentPresenter Grid.Row="2" Margin="5" HorizontalAlignment="Center"
                                  Content="{TemplateBinding Tag}">
                </ContentPresenter>




                <!-- The Resize Grip. -->
                <ResizeGrip Name="WindowResizeGrip" Grid.Row="2" 
                            HorizontalAlignment="Right" VerticalAlignment="Bottom"
                            Visibility="Collapsed" IsTabStop="False"/>

                <!--Resize Right Rectangle -->
                <Rectangle Grid.Row="1" Grid.RowSpan="3"
                            Cursor="SizeWE" Fill="Transparent" Width="5" VerticalAlignment="Stretch" HorizontalAlignment="Right"
                            MouseLeftButtonDown="window_initiateResizeWE"
                            MouseLeftButtonUp="window_endResize"
                            MouseMove="window_Resize"/>

                <!--Resize Right Rectangle -->
                <Rectangle Grid.Row="2" 
                            Cursor="SizeNS" Fill="Transparent" Height="5" HorizontalAlignment="Stretch" VerticalAlignment="Bottom"
                            MouseLeftButtonDown="window_initiateResizeNS"
                            MouseLeftButtonUp="window_endResize"
                            MouseMove="window_Resize"/>
            </Grid>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="ResizeMode" Value="CanResizeWithGrip">
                <Setter TargetName="WindowResizeGrip" Property="Visibility" Value="Visible"/>
            </Trigger>

            <!--<EventTrigger RoutedEvent="Loaded">
                <EventTrigger.Actions>
                    <StaticResource ResourceKey="beginStoryboard"/>
                </EventTrigger.Actions>
            </EventTrigger>-->
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="StyleOperateWindow" TargetType="{x:Type Window}">
        <Setter Property="AllowsTransparency" Value="True"/>
        <Setter Property="WindowStyle" Value="None"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Opacity" Value="1"/>
        <Setter Property="Template" Value="{StaticResource TemplateCustomWindow}"/>
        <Setter Property="MinWidth" Value="200"/>
        <Setter Property="MinHeight" Value="100"/>
        <!--<Setter Property="Topmost" Value="True"/>-->
        <Setter Property="BorderBrush" Value="{StaticResource LGBrushDrakBlue}"/>
        <Setter Property="Background" Value="{StaticResource RGBrushLightSkyBlue}"/>
        <Style.Triggers>
            <EventTrigger RoutedEvent="Window.Loaded">
                <EventTrigger.Actions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0" Duration="0:0:0.5" AccelerationRatio="0.7"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger.Actions>
            </EventTrigger>
        </Style.Triggers>
    </Style>

    <ControlTemplate x:Key="TemplateRoundWindow" TargetType="{x:Type Window}">
        <Border Name="windowFrame" BorderThickness="1" Margin="15"
                Width="Auto" Height="Auto"
                CornerRadius="10, 10, 0, 10" Background="{TemplateBinding BorderBrush }">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="5"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!--The title bar-->
                <TextBlock Margin="1" Padding="5" Text="{TemplateBinding Title}" FontWeight="Bold" Foreground="White"
                           MouseLeftButtonDown="titlebar_MouseLeftButtonDown">
                </TextBlock>
                <!--Close Button-->
                <Button HorizontalAlignment="Right" Click="cmdClose_Click" Margin="5" Style="{StaticResource StyleCloseButton}">
                    X
                </Button>

                <!-- The Window Content. -->
                <Border Grid.Row="1" Background="{TemplateBinding Background }">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <AdornerDecorator>
                            <ContentPresenter>

                            </ContentPresenter>
                        </AdornerDecorator>

                        <!--<StackPanel Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Bottom" Orientation="Horizontal">
                            <Button Name="btnCancel" Content="Cancel" Margin="15, 5" Padding="15, 10" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="Cancel_Click"/>
                            <Button Name="btnOK" Content="OK" Margin="5" Padding="15, 10" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="OK_Click"/>
                        </StackPanel>-->
                    </Grid>
                </Border>

                <!-- The footer -->
                <ContentPresenter Grid.Row="2" Margin="5" HorizontalAlignment="Center"
                                  Content="{TemplateBinding Tag}">
                </ContentPresenter>




                <!-- The Resize Grip. -->
                <ResizeGrip Name="WindowResizeGrip" Grid.Row="2" 
                            HorizontalAlignment="Right" VerticalAlignment="Bottom"
                            Visibility="Collapsed" IsTabStop="False"/>

                <!--Resize Right Rectangle -->
                <Rectangle Grid.Row="1" Grid.RowSpan="3"
                            Cursor="SizeWE" Fill="Transparent" Width="5" VerticalAlignment="Stretch" HorizontalAlignment="Right"
                            MouseLeftButtonDown="window_initiateResizeWE"
                            MouseLeftButtonUp="window_endResize"
                            MouseMove="window_Resize"/>

                <!--Resize Right Rectangle -->
                <Rectangle Grid.Row="2" 
                            Cursor="SizeNS" Fill="Transparent" Height="5" HorizontalAlignment="Stretch" VerticalAlignment="Bottom"
                            MouseLeftButtonDown="window_initiateResizeNS"
                            MouseLeftButtonUp="window_endResize"
                            MouseMove="window_Resize"/>
            </Grid>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="ResizeMode" Value="CanResizeWithGrip">
                <Setter TargetName="WindowResizeGrip" Property="Visibility" Value="Visible"/>
            </Trigger>

            <!--<EventTrigger RoutedEvent="Loaded">
                <EventTrigger.Actions>
                    <StaticResource ResourceKey="beginStoryboard"/>
                </EventTrigger.Actions>
            </EventTrigger>-->
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="StyleCustomWindow" TargetType="{x:Type Window}">
        <Setter Property="AllowsTransparency" Value="True"/>
        <Setter Property="WindowStyle" Value="None"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Opacity" Value="0.95"/>
        <Setter Property="Template" Value="{StaticResource TemplateRoundWindow}"/>
        <Setter Property="MinWidth" Value="100"/>
        <Setter Property="MinHeight" Value="70"/>
        <Setter Property="Topmost" Value="True"/>
        <Setter Property="BorderBrush" Value="{StaticResource LGBrushKhaki}"/>
        <Setter Property="Background" Value="{StaticResource RGBrushLightSkyBlue}"/>
        <Style.Triggers>
            <EventTrigger RoutedEvent="Window.Loaded">
                <EventTrigger.Actions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0" Duration="0:0:0.7" AccelerationRatio="0.8"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger.Actions>
            </EventTrigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>